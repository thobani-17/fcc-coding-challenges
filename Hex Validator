Hex Validator (23-01-2026)

def is_valid_hex(s):
    import string
    
    if s[0] != "#":
        return False
    if len(s[1:]) == 6 and all(char in string.hexdigits for char in s[1:]):
        return True
    if len(s[1:]) == 3 and all(char in string.hexdigits for char in s[1:]):
        return True
    return False
