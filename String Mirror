String Mirror (23-09-2025)

def is_mirror(str1, str2):
    import string
    str1 = str1.replace(" ", "")
    for char in str2:
        if char not in string.ascii_letters:
            str2 = str2.replace(char, "")
    if str2 == str1[::-1]:
        return True
    else:
        return False
