2026 Winter Games Day 11: Ice Hockey (16-02-2026)

def get_semifinal_matchups(teams):
    pairs = []
    for item in teams:
        country = item[:3]
        points = int(item[-3])*1 + int(item[-5])*2 + int(item[-7])*3
        pairs.append((points, country))
    pairs.sort()
    pairs.reverse()
    return f"The semi-final games will be {pairs[0][1]} vs {pairs[3][1]} and {pairs[1][1]} vs {pairs[2][1]}."
